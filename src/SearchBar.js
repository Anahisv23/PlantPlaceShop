// will integrate an api here or allow users to search our stock

const SearchBar = () => {
  return (
    <div>
      <input
        type="text"
        placeHolder="Search plant here"
        // onClick=
        // value="val"
      ></input>
      <button>
        <span role="img">🔍</span>
      </button>
    </div>
  );
};

export default SearchBar;
